version: '3.3'

services:
  check-white-screen:
    build: .
    container_name: check-white-screen
    ports:
      - "${HOST_PORT:-33223}:${PORT:-33223}"
    volumes:
      - ./data:/app/data
      - ./.env:/app/.env:ro
    restart: unless-stopped
    environment:
      - NODE_ENV=production
